add_library(bux-mariadb STATIC oo_mariadb.cpp)
#target_compile_options(bux-mariadb PRIVATE -DCLT_DEBUG_)
target_include_directories(bux-mariadb PRIVATE ../include ../../bux/include)
if(CMAKE_CXX_COMPILER_ID STREQUAL "MSVC")
target_link_libraries(bux-mariadb PRIVATE fmt::fmt)
endif()
